/**
 * 配置文件模板
 */

export default {
  // `baseURL` 将自动加在 `url` 前面，除非 `url` 是一个绝对 URL。
  baseURL: 'http://mt.com/api',
  // baseURL: 'http://mt.test.btcguangzhou.xyz/api',
  //图片请求地址前缀, 除非 `url` 是一个绝对 URL。
  baseImgURL: 'http://mt.com/upload/',
  // baseImgURL: 'http://mt.test.btcguangzhou.xyz/upload/',
  // 分时图请求地址，除非 `url` 是一个绝对 URL。
  baseKLineURL: 'http://mt-tp.test.btcguangzhou.xyz/',
  // baseKLineURL: 'http://mt-tp.test.btcguangzhou.xyz',
}